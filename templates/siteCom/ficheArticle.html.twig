{# templates/siteCom/index.html.twig #}
{% extends 'base.html.twig' %}
  {% block stylesheets %}
        {{parent()}}
  {% endblock %}
  {% block body %}
      {% block header %}
        {{parent()}}
      {% endblock %}
      {% block detailArticle %}
      <!-- Header End====================================================================== -->
      <div id="mainBody">
      	<div class="container">
      	<div class="row">

      	<div class="span9">
          <ul class="breadcrumb">
          <li><a href="{{path('siteCom')}}">Accueil</a> <span class="divider">/</span></li>
          <li class="active">Details</li>
          </ul>
                  	<div class="row">
                      {% for article in produit %}
                  			<div id="gallery" class="span3">
                  				<img src="{{article.getAdresPhoto}}" style="width:100%" alt="Fujifilm FinePix S2950 Digital Camera"/>
                  			 <div class="btn-toolbar">
                  			  <div class="btn-group">
                  				<span class="btn"><i class="icon-envelope"></i></span>
                  				<span class="btn" ><i class="icon-print"></i></span>
                  				<span class="btn" ><i class="icon-zoom-in"></i></span>
                  				<span class="btn" ><i class="icon-star"></i></span>
                  				<span class="btn" ><i class=" icon-thumbs-up"></i></span>
                  				<span class="btn" ><i class="icon-thumbs-down"></i></span>
                  			  </div>
                  			</div>
                  			</div>
      			<div class="span6">
      				<h3>{{article.designation}}</h3>
      				<hr class="soft"/>
      			
      					<label class="control-label"><span>{{article.getPrix}} €</span></label>
      					<div class="controls">

                {{ form_start(formPanier, {'attr': {'class': 'form-horizontal qtyFrm'} }) }}
                  {{ form_widget(formPanier) }}
                  {{ form_end(formPanier) }}
      				
      					</div>
      				  </div>
      				<hr class="soft clr"/>
      				<p>
                  {{article.description}}
      				</p>
      			<a href="#" name="detail"></a>
      			<hr class="soft"/>
      			</div>
              {% endfor %}

      	
      </div>
      </div> </div>
      </div>
      <!-- MainBody End ============================= -->
      <!-- MainBody End ============================= -->
      {% endblock %}

      {% block footer %}
        {{parent()}}
        {% endblock %}
  {% endblock %}
